# --------------------
# Define default values
# --------------------

# ==================== Alas ====================

# ==================== Farm ====================

OperationHandover:
  Scheduler:
    Enable: false
    Command: OperationHandover
    SuccessInterval: 30-60
    FailureInterval: 30-60

# ==================== Event ====================

EventA:
  EventDaily:
    StageFilter: |-
      A1 > A2 > A3
EventB:
  EventDaily:
    StageFilter: |-
      B1 > B2 > B3
EventC:
  EventDaily:
    StageFilter: |-
      C1 > C2 > C3
EventD:
  EventDaily:
    StageFilter: |-
      D1 > D2 > D3

# ==================== Reward ====================

# ==================== DailyReward ====================

# ==================== DailyMission ====================

# ==================== Opsi ====================
ThreeOilLowCost:
  Scheduler:
    Enable: false
OpsiCrossMonth:
  Scheduler:
    Sensitive: true
OpsiObscure:
  Scheduler:
    Sensitive: true
OpsiAbyssal:
  Scheduler:
    Sensitive: true
